from ..mqtt import MQTTClient

ONE_SHOT = False



def start(client):
    # If button is pressed?
    # If relay is triggered?

    client.
    client.subscribe('HomeKit/{}/Lightbulb/On'.format(client.client_id))